const superHeroes: string[] = [] //saying that this will be a type of string array
// const heroPower: number[] = []
const heroPower: Array<number> = [] //another way of defining the same code above

type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [];

const MLModels: number[][] =[
    [255,255,255],
    []
]

superHeroes.push('Spiderman')
heroPower.push(2)

allUsers.push({name:"", isActive: true})